buildscript {
  ext.deps = [
          junit: 'junit:junit:4.12'
  ]

  repositories {
    mavenCentral()
  }
}

subprojects { projects ->
  group = GROUP
  version = VERSION_NAME

  repositories {
    mavenCentral()
  }

  apply plugin: 'java'
}
